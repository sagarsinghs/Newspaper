<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sangamsagar.newspapertime">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".stockmarkets.StockPriceActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".RoomDatabase.RoomMainActivity" />
        <activity android:name=".TabFragments.DisplayingTheDetails" />
        <activity android:name=".DatabaseMainClass" />
        <activity android:name=".Countries.India" />
        <activity android:name=".PopularChannels.Fragments.ChannelsExpand" />
        <!-- <activity android:name=".BaseActivity" /> -->
        <activity
            android:name=".BottomActivity"
            android:label="@string/title_activity_bottom" />
        <activity android:name=".NewActivityCreatedTesting"
            android:screenOrientation="portrait"/>
        <activity android:name=".PopularChannels.Fragments.NewsChannels"></activity>
    </application>

</manifest>